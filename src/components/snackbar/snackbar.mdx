---
name: Snackbar
route: /components/snackbar
menu: Components
---

import { Playground, PropsTable } from 'docz'
import Button from 'components/button'
import Snackbar from './snackbar'

# Snackbar

## Basic usage

<Playground className="vertical-separator">
  <Snackbar
    defaultOpen
    content="Default Snackbar"
  />

  <Snackbar
    defaultOpen
    content="Success Snackbar"
    success
  />

  <Snackbar
    defaultOpen
    content="Warning Snackbar"
    warning
  />

  <Snackbar
    defaultOpen
    content="Error Snackbar"
    error
  />
</Playground>

## Auto-close

<Playground>
  <Snackbar
    autoClose
    delay={2000}
    content="Snackbar automatically closed after 2 seconds"
    renderTrigger={({ show, isOpen }) => (
      !isOpen && (
        <Button onClick={show}>
          Open auto-close Snackbar
        </Button>
      )
    )}
  />
</Playground>

## Properties

<PropsTable of={Snackbar} />
