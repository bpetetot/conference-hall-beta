---
name: Confirmation Popin
route: /portals/confirmation
menu: Portals
---

import { Playground, PropsTable } from 'docz'
import ConfirmationPopin from './confirmation'

# Confirmation Popin

## Basic usage

<Playground>
  <ConfirmationPopin
    open
    question="Can you confirm by clicking on OK, else click Cancel ?"
    className="default-theme"
    onOk={console.log}
    onCancel={console.log}
    renderTrigger={({ show }) => (
      <button onClick={show} className="btn btn-primary">
        Click to open
      </button>
    )}
  />
</Playground>

## Properties

<PropsTable of={ConfirmationPopin} />
