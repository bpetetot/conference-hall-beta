---
name: Drawer
route: /portals/drawer
menu: Portals
---

import { Playground, PropsTable } from 'docz'
import Drawer from './drawer'

# Drawer

## Basic usage

<Playground>
  <Drawer
    title="title"
    subtitle="subtitle"
    actions={({ hide }) => (
      <button onClick={hide} className="btn btn-primary">
        Action
      </button>
    )}
    renderTrigger={({ show }) => (
      <button onClick={show} className="btn btn-primary">
        Click to open
      </button>
    )}
  >
    This is a drawer!
  </Drawer>
</Playground>

## Properties

<PropsTable of={Drawer} />
